# Password Generator

This is a simple Python project that generates strong random passwords using letters, numbers, and symbols.

## How it works
- You enter the password length
- The program chooses random characters
- A secure password is generated

## How to Run
```bash
python password_generator.py
